# Database Setup

1. Create a project in [Supabase](https://supabase.com).
2. Go to the SQL Editor in your Supabase dashboard.
3. Paste the contents of `schema.sql` and run it to create tables.
4. (Optional) Run `seed.sql` to verify tables were created successfully.
5. Grab your database URL and Service Role Key for `.env`.
